{% extends 'base.html.twig' %}

{% block title %}Modifier Voiture{% endblock %}

{% block body %}
    <div class="p-12">

        <div class="flex gap-4 items-center">
            <h1 class="text-3xl font-semibold mb-6">Modifier Voiture</h1>
            <div>
                {{ include('voiture/_delete_form.html.twig') }}
            </div>
        </div>


        <div class="bg-white p-6 rounded-lg shadow-md mb-6">
            {{ include('voiture/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <a href="{{ path('app_voiture_index') }}" class="text-blue-600 hover:underline mb-4 inline-block">Retour à la liste</a>


    </div>

{% endblock %}
