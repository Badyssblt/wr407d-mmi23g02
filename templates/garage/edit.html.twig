{% extends 'base.html.twig' %}

{% block title %}Edit Garage{% endblock %}

{% block body %}
    <div class="p-12">

        <div class="flex gap-4 items-end mb-6">
            <h1 class="text-3xl font-bold">Edit Garage</h1>
            <div>
                {{ include('garage/_delete_form.html.twig') }}
            </div>
        </div>

        {{ include('garage/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="mt-6 flex space-x-4">
            <a href="{{ path('app_garage_index') }}" class="text-blue-500 hover:text-blue-700">Back to list</a>
        </div>

    </div>
{% endblock %}
